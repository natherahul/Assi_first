#include<stdio.h>
int main()
{
    int a,b,c,d;
    a=sizeof(float);
    b=sizeof(int);
    c=sizeof(char);
    d=sizeof(double);
    printf("size of a variable is %d\n",a);
    printf("size of b variable is %d\n",b);
    printf("size of c variable is %d\n",c);
    printf("size of d variable is %d\n",d); 
}